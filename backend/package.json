{
  "name": "fuel-eu-backend",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "node -e \"console.log('backend dev placeholder — real server will be added in Part 5')\"",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev --name init",
    "prisma:studio": "prisma studio",
    "prisma:seed": "ts-node prisma/seed.ts",
    "seed": "npm run prisma:generate && npm run prisma:migrate && npm run prisma:seed",
    "...": "...",
    "test": "jest --runInBand"
  },
  "dependencies": {
    "@prisma/client": "^6.19.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "jest": "^29.7.0",
    "prisma": "^6.19.0",
    "ts-jest": "^29.4.5",
    "ts-node": "^10.9.1",
    "typescript": "^5.9.3"
  }
}
